name: temp_fuel_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.2.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.0
  url_launcher: ^6.3.0
  crypto: ^3.0.3
  cupertino_icons: ^1.0.8
  intl: ^0.20.2
  fl_chart: ^0.69.0
  csv: ^6.0.0
  file_picker: ^8.1.2

  sqflite: ^2.3.0
  sqflite_common_ffi: ^2.3.2
  path: ^1.9.0
  path_provider: ^2.1.4
  supabase_flutter: ^2.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/fuelflow_logo.png